import { def } from "@vue/shared";
import { Router } from "express";

const routes = Router();

//routes.get("/", null);
//routes.post("/notas-ficais", null);
//routes.post("/notas-ficais/new", null);
//routes.post("/notas-ficais/:id", null);

export default routes;
